{
  "name": "@claude/cursor-harness-mcp",
  "version": "0.1.0",
  "description": "Dispatch Cursor CLI subagents from Claude Code (or any MCP-compatible client).",
  "type": "module",
  "license": "SEE LICENSE IN LICENSE",
  "bin": {
    "cursor-harness-mcp": "dist/server.js"
  },
  "main": "dist/server.js",
  "files": [
    "dist",
    "plugin",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
